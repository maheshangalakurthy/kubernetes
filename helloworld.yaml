apiVersion: v1
kind: Pod
metadata:
  name: helloworld
  labels:
    name: jenkins
spec:
  containers:
  - name: jenkins
    image: jenkins
    resources:
      limits:
        memory: "1028Mi"
        cpu: "1000m"
    ports:
      - containerPort: 8080
